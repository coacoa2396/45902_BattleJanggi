using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//StartButton을 누를 경우 씬 전환(말 배치 화면으로)
//OptionButton을 누르면 SoundOnButton과 BGMOnButton이 나타남
//SoundOnButton을 누르면 SoundOffButton으로 교체되고 효과음이 음소거
//BGMOnButton을 누르면 BGMOffButton으로 교체되고 BGM이 음소거


public class ReadMe : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
