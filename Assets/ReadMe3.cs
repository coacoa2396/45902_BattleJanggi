using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Continue Button을 누르면 GameStartUI로 넘어감
//Close Button을 누르면 게임이 꺼짐

public class ReadMe3 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
