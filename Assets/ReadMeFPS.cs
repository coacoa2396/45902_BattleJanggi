using System.Collections;
using System.Collections.Generic;
using UnityEngine;


// 1P와 2P를 Panel로 UI를 구분함
// Image는 건드리지 않음
// Bullet Text는 현재 탄환수 / 총 탄환수를 표시함
// Skill CoolTime은 노란색 Outline이 원을 이루면 스킬 사용 가능 상태
// 원이 채워지며 쿨타임을 유저가 인식할 수 있도록 함
// Reload Text의 경우 리로드할 때 Visible을 True로 변환함 (3초간 깜빡이면 좋겠지만, DOTween을 쓸 수 없는 패키지를 썼으므로 어렵다면 유기 가능)


public class ReadMeFPS : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
